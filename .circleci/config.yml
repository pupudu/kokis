version: 2
jobs:
  publish:
    docker:
      - image: 'circleci/node:latest'
    steps:
      - checkout
      - run: yarn
      - run: GH_TOKEN=${GH_TOKEN} yarn lerna:publish

workflows:
  version: 2
  main:
    jobs:
      - publish:
          context: kokis
